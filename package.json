{
  "name": "web3-notes-backend",
  "version": "1.0.0",
  "description": "Backend API for Web3 Notes faucet functionality",
  "main": "api/faucet.js",
  "scripts": {
    "dev": "vercel dev",
    "build": "vercel build",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "ethers": "^6.8.0"
  },
  "devDependencies": {
    "@vercel/node": "^3.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}