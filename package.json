{
  "name": "chainnotes",
  "version": "1.0.0",
  "description": "ChainNotes - Decentralized note-taking app with auto-faucet",
  "scripts": {
    "dev:frontend": "cd frontend && npm run dev",
    "build:frontend": "cd frontend && npm run build",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "ethers": "^6.8.0",
    "vercel": "^48.10.2"
  },
  "devDependencies": {
    "@vercel/node": "^3.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
